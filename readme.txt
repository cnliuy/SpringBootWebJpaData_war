基于 spring boot 构建

1. eclispe 中可以使用sample.jpa 下的  SampleJpaApplication 运行程序

2. 在 sample.aop 下的 SampleAopApplication 测试 AOP

可以使用  mvn install 形成 war包 ，放到 tomcat中使用。

Eclisep 里的 github 更新
 |--project
 |----Team
 |------Remote
 |--------Push。。。
 |----------master[branch]  “Add Spec”
 
 
 注:
 commit 提交到本地
 push   推动到github
 
 打算添加AOP测试
 
 --EOF--
