基于 spring boot 构建
eclispe 中可以使用 SampleJpaApplication 运行程序
可以使用  mvn install 形成 war包 ，放到 tomcat中使用。

Eclisep 里的 github 更新
 |--project
 |----Team
 |------Remote
 |--------Push。。。
 |----------master[branch]  “Add Spec”
 
 
 
 --EOF--
